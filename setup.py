from distutils.core import setup

setup(name="text_classify",
      version="0.0",
      description="a set of text classification packages",
      author="Eric Schles",
      packages=["text_classify"]
  )
